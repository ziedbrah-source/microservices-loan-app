eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka


server:
  port: 8083
spring:
  application:
    name: ocr-service


management:
  tracing:
    sampling:
      probability: 1.0
